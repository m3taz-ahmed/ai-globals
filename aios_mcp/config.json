{
  "mcpServers": {
    "ai-global-os": {
      "command": "python",
      "args": [
        "-c",
        "import os,sys,subprocess,pathlib; root=os.environ.get('AGENT_OS_ROOT') or str(pathlib.Path.cwd()); subprocess.run([sys.executable,'-m','aios_mcp.aios_server'], cwd=root)"
      ]
    }
  }
}
