[TECH] Go 1.26 (released Feb 2026, latest 1.26.5 Jul 2026)
[OBJ] Go programming language ط¸آ€آ¤ `new(expr)` initializer, self-referential generic constraints, Green Tea GC default, ~30% cgo overhead reduction, `go fix` modernizers, new crypto packages, `errors.AsType`, `simd/archsimd`, `runtime/secret`.
[RULES]
1. [REQ] Use `new(expr)` initializer to allocate + initialize in one expression: `p := new(MyType{Field: val})` replaces `&MyType{Field: val}` for clarity.
2. [REQ] Use self-referential generic constraints for recursive type bounds: `type Tree[T any] interface { ~*Node[T] }`.
3. [REQ] Green Tea GC is default ط¸آ€آ¤ no `GOGC` tuning needed for most workloads; profile before overriding.
4. [REQ] Use `go fix` modernizers to auto-migrate code to latest idioms: `go fix ./...` applies all modernizers.
5. [REQ] Use `crypto/hpke` for hybrid public-key encryption (new in 1.26) ط¸آ€آ¤ replace ad-hoc crypto for key exchange + AEAD.
6. [REQ] Use `testing/cryptotest` for cryptographic implementation testing ط¸آ€آ¤ standardized test vectors + property tests.
7. [REQ] Use `log/slog.NewMultiHandler` to fan-out logs to multiple handlers: `slog.New(slog.NewMultiHandler(h1, h2))`.
8. [REQ] Use `errors.AsType(err, &target)` for typed error extraction (replaces `errors.As` for type-only matching).
9. [REQ] Use `simd/archsimd` for architecture-specific SIMD intrinsics ط¸آ€آ¤ prefer over hand-rolled assembly.
10. [REQ] Use `runtime/secret` for sensitive memory handling ط¸آ€آ¤ zeroing, mlock where available.
11. [REQ] Maintain Go 1 compatibility ط¸آ€آ¤ code must compile on prior Go 1.x releases unless using new APIs.
12. [REQ] Use `go mod tidy` + `go mod verify` before commits.
13. [REQ] Use `context.Context` as first parameter in all public functions taking requests.
14. [REQ] Use `errors.Is` / `errors.As` / `errors.AsType` for error handling ط¸آ€آ¤ never string-compare errors.
15. [PROHIBIT] Never disable Green Tea GC via `GOGC=off` in production without profiling justification.
16. [PROHIBIT] Never use `unsafe` for SIMD ط¸آ€آ¤ use `simd/archsimd`.
17. [PROHIBIT] Never hand-roll crypto ط¸آ€آ¤ use `crypto/hpke`, `crypto/*` stdlib packages.
18. [CMD] `go fix ./...` ط¸آ€آ¤ auto-migrate to 1.26 idioms.
19. [CMD] `go test ./... -cover` ط¸آ€آ¤ full test suite with coverage.
20. [CMD] `go build ./...` ط¸آ€آ¤ compile all packages.
[COMPAT]
- Go 1.26 maintains Go 1 compatibility promise.
- ~30% cgo overhead reduction vs 1.25.
- Green Tea GC default (opt-out via GOGC).
- New: `crypto/hpke`, `testing/cryptotest`, `log/slog.NewMultiHandler`, `errors.AsType`, `simd/archsimd`, `runtime/secret`.
- `go fix` modernizers: auto-migrate deprecated patterns.
[REFS]
- https://go.dev/doc/go1.26
- https://pkg.go.dev/crypto/hpke
- https://pkg.go.dev/testing/cryptotest
- https://pkg.go.dev/log/slog
- https://go.dev/ref/spec
