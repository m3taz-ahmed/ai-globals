# Node.js 22 LTS Standards
- **Execution:** Utilize `node --run` for executing package.json scripts (faster than npm run).
- **Security:** Always use `--experimental-permission` flags to lock down file system and network access for secure APIs.