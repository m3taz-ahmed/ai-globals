# Tailwind CSS v4.1 Standards
- **Engine:** Utilize the oxidized (Rust-based) engine implicitly.
- **Nesting & CSS:** Use advanced native CSS nesting seamlessly with `@theme` configurations. Avoid complex `@apply` chains; use standard utility classes in HTML.