# Bandit security scanner configuration for AI Global OS.
# https://bandit.readthedocs.io/en/latest/config.html

# Skip B101 (assert_used) — pytest tests use assert extensively by design.
# Skip B404 (subprocess) — MCP client requires subprocess for stdio transport.
# Skip B603 (subprocess_without_shell_equals_true) — intentional for MCP.
# Skip B608 (hardcoded_sql_expressions) — all dynamic SQL uses parameterized
#   queries with validated identifier whitelists (see P0.1 fix).
skips: ['B101', 'B404', 'B603', 'B608']

# Exclude test directories and generated files from scanning.
exclude_dirs:
  - "tests"
  - "runtime/tests"
  - "memory/tests"
  - "skills"
  - "tmp"
  - "scratch"
  - "temp"
  - "brain"
  - "state"
  - "__pycache__"
  - ".git"
  - ".github"
  - "graphify-out"
