{
  "mcpServers": {
    "ai-global-os": {
      "command": "python",
      "args": [
        "-c",
        "import os,sys,subprocess,pathlib; root=os.environ.get('AGENT_OS_ROOT'); root=root if (root and pathlib.Path(root).is_dir()) else (str(pathlib.Path.cwd()) if pathlib.Path.cwd().joinpath('config.py').exists() else 'D:/.ai'); os.environ['AGENT_OS_ROOT']=root; os.environ.setdefault('PYTHONIOENCODING','utf-8'); subprocess.run([sys.executable,'-m','aios_mcp.aios_server'], cwd=root)"
      ]
    },
    "graphify": {
      "command": "python",
      "args": [
        "-c",
        "import os,sys,subprocess,pathlib; root=os.environ.get('AGENT_OS_ROOT'); root=root if (root and pathlib.Path(root).is_dir()) else (str(pathlib.Path.cwd()) if pathlib.Path.cwd().joinpath('config.py').exists() else 'D:/.ai'); os.environ['AGENT_OS_ROOT']=root; os.environ.setdefault('PYTHONIOENCODING','utf-8'); subprocess.run([sys.executable, str(pathlib.Path(root)/'scripts'/'graphify_mcp_wrapper.py')])"
      ]
    },
    "upwork": {
      "command": "npx",
      "args": ["-y", "@furkankoykiran/upwork-mcp"],
      "env": {
        "UPWORK_CLIENT_ID": "${UPWORK_CLIENT_ID}",
        "UPWORK_CLIENT_SECRET": "${UPWORK_CLIENT_SECRET}"
      }
    },
    "freelancer": {
      "command": "npx",
      "args": ["-y", "freelancer-mcp-server"],
      "env": {
        "FREELANCER_OAUTH_TOKEN": "${FREELANCER_OAUTH_TOKEN}"
      }
    },
    "fiverr": {
      "command": "uvx",
      "args": ["fiverr-mcp-server"]
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"]
    },
    "linkedin": {
      "command": "octopus-linkedin-mcp",
      "args": []
    }
  }
}
