[FILE] commands-reference
[OBJ] Comprehensive Slash Commands Directory.
[RULES]
1. [REQ] Centralized documentation for all `/{command}` shortcuts.
2. [REQ] Covers IDE Core (`/goal`, `/schedule`, `/grill-me`).
3. [REQ] Covers OS Workflows (`/plan`, `/execute`, `/review`, `/debug`, `/deploy`, `/prompt`, `/sync`).
4. [REQ] Covers Audits (`/maintain`, `/audit`, `/refactor`, `/security`).
5. [REQ] Covers Ponytail Skills (`/ponytail`, `/ponytail lite`, `/ponytail ultra`, `/ponytail-review`, `/ponytail-gain`, `/ponytail off`).
